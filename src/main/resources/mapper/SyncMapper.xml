<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sync.demo.mapper.SyncMapper">

    <!-- 目的：为dao接口方法提供sql语句配置 -->
    <select id="queryList" parameterType="String" resultType="HashMap">
        ${value}
    </select>

    <select id="getListId" parameterType="String" resultType="Integer">
        ${value}
    </select>

    <select id="executeSql" parameterType="String" >
        ${value}
    </select>

</mapper>
